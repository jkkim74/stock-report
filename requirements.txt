# pip install -r requirements.txt
#
# beautifulsoup4 / lxml은 report_generator.py 안에서 지연 임포트되어 눈에 띄지 않지만
# 필수다. KRX 조회가 실패했을 때 타는 Naver 폴백 경로가 이 둘을 사용한다.

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
pykrx>=1.0.45
yfinance>=0.2.40
finance-datareader>=0.9.50
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0
tqdm>=4.65
pytz>=2023.3
schedule>=1.2
slack_sdk>=3.21
